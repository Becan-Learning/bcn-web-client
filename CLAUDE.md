@AGENTS.md

> **ابدأ من [docs/STATE.md](docs/STATE.md)** — حالة المشروع، ما بُني وما بقي،
> الانحرافات المتعمَّدة عن هذه الوثيقة، والمزالق التقنية المكتشفة.

# بيكان — Becan

منتج ويب عربي RTL يشرح للطالب الجامعي مقرره الدراسي بالصوت عبر سبورة تفاعلية
يكتب عليها الوكيل أثناء الشرح، ثم يسأله ليتأكد أنه فهم.

الاسم على الواجهة: **بيكان / Becan** · الشعار: **Becan. You Can**
الطالب **لا يرفع محتوى** — يجد مادته جاهزة ويختارها فقط.

## الستاك
Next.js 16 · TypeScript · Tailwind 4 · Motion (الحزمة `motion`، الاستيراد من `motion/react` — **ليست** `framer-motion`)
· Radix (`radix-ui`) للآليات وحدها · LiveKit · react-pdf · Bunny · Langfuse · pnpm

## هيكل المشروع — بعد الدمج (2026-09-14)
المشروع دمجٌ لمرجعين في `ref/`: **`becan-design`** مصدر الشكل كله، و**`old-sanad`** مصدر محرّك الجلسة.
عند الشكّ في شكلٍ ارجع إلى الأول، وفي سلوك الوكيل إلى الثاني.

| المسار | المحتوى |
|---|---|
| `src/app/` | كل الشاشات (بيانات عرض) + `api/` (رمز LiveKit · وسيط Bunny · تقييم Langfuse) |
| `src/components/becan/` | عُدّة التصميم: `kit` · `icons` · `logo` · `becan-face` … |
| `src/components/session/` | الجلسة الحقيقية: `session-view` · `parts` · `slides-pdf` |
| `src/lib/data/` | بيانات العرض: الكتالوج · الخطط · الفوترة · الأسئلة · الدعم |
| `src/lib/session/` | عقد الوكيل (`ui-control` · `teaching-board`) · `session-reducer` · `content` |
| `src/styles/` | `tokens.css` + `globals.css` |

- **عقد الوكيل لا يُغيَّر من طرف واحد:** الوكيل ينشر على موضوع `ui-control`
  (`scroll` · `set_lesson` · `set_topic` · `topic_done` · `session_ending` · `board_*`)،
  والطالب يكتب على `lk.chat`. سمات المشارك في `api/get-lk-token`.
- **Radix للآليات فقط** (حبس التركيز · Escape · البوابات) ويُلوَّن بالتوكنات. لا مظهر shadcn.
  نوافذ الجلسة داخل البوابة تعيد `data-theme="dark" data-surface="session"`.
- **أدوات الجلسة التي لا يدعمها الوكيل بعد** (السرعة · الإيقاف · أعد · ما فهمت · ابدأ من هنا)
  ظاهرة بحدّ متقطّع ووسم «قريبًا» — لا تُخفى ولا تُزيَّف.
- Clerk وSupabase **غير مركّبين بعد**؛ المعرّف مجهول (`becan:uid`) والكتالوج عرض.
- التحقق: `pnpm lint` ثم `pnpm type-check` ثم `pnpm build`.

## الهوية — مقفلة، لا تُقترح بدائل
كل الألوان من متغيرات CSS في `src/styles/tokens.css`.
**صفر قيمة لون صريحة (hex/rgb) في أي مكوّن.**
الخط: IBM Plex Sans Arabic — لا يُقترح خط آخر ولا اقتران خطوط.

### ثلاثة أدوار لا تتداخل أبدًا
- **المسرح** = الباذنجاني → خلفيات · أقسام كبيرة · أرضية التطبيق · رؤوس
- **الفعل** = الكهرماني → العناصر القابلة للضغط **فقط**. لا عناوين ولا أيقونات زينة
- **الدفء** = الجوزي → شعار · فواصل · رسوم. **لا يحمل نصًا أبدًا** (تباين 3.07:1)

`--live` الأخضر = «يحدث الآن» + إجابة صحيحة — **ممنوع كلون هوية**
`--error` النبيذي = خطأ فقط — **ممنوع للوقت أو الإلحاح**
`--aubergine-soft` ممنوع بجوار `--error` في نفس السياق

## RTL — إلزامي
- خصائص منطقية فقط. في Tailwind: `ms-` `me-` `ps-` `pe-` `start-` `end-`
- **ممنوع** `ml-` `mr-` `pl-` `pr-` `left-` `right-` وأي `margin-left/right` في CSS
- رمز لاتيني داخل نص عربي يُلَفّ بـ `<span dir="ltr">` وإلا انقلب
- الأسهم والشيفرونات تنعكس · الساعة والميكروفون لا تنعكس
- **أرقام إنجليزية (0–9)** في كل الواجهة — لا أرقام هندية

## Mobile-first
390px أولًا → 768 → 1120. أهداف اللمس ≥ 44×44px.
`:focus-visible` مرئي على كل عنصر تفاعلي. `prefers-reduced-motion` مطبَّق دائمًا.
ارتفاع السطر للنص العربي ≥ 1.7. عرض المتن ≤ 74 حرفًا.

## الشعور المستهدف
**«أنا ماسك الوضع»** — الانتقال من مبعثر ومتأخر إلى مسيطر وعارف بموقعه.
اختبار كل عنصر: *هل يزيد إحساسه بالسيطرة؟* إن لا — احذفه مهما كان جميلًا.

## أنماط بيكان العشرة — تُطبَّق على كل شاشة
1. **رقم واحد بارز** لكل شاشة — لا لوحة إحصائيات
2. **السبورة تُكتب تدريجيًا** متزامنة مع الصوت — لا سطر يظهر قبل نطقه
3. **مؤشر الصوت نبضة هادئة** (2–3ث للدورة) بالأخضر، ونصّه «يشرح الآن» لا «جارٍ التشغيل»
4. **بيانات حقيقية بدل ادّعاءات** — رمز المادة، الجامعة، عدد الفصول
5. **الطالب فاعل الجملة**: «أنجزت 3 فصول» لا «بيكان شرح لك»
6. **الملخص يذكر التعثّر** لا المدح — صفر إيموجي احتفالي
7. **الإلحاح كهرماني هادئ + فعل ممكن اليوم** — لا إنذار أحمر
8. **زر كهرماني أساسي واحد** لكل شاشة، والبقية روابط أو أزرار محدّدة الإطار
9. **الموقع فاتح · الجلسة داكنة** — الانتقال تلاشٍ 250ms لا وميض
10. على الجوال **كل تحكّم في الجلسة يسكن الثلث السفلي** (متناول الإبهام)

## قواعد على الأدوات
- **21st MCP:** للآليات فقط (قوائم منسدلة · بحث بالاقتراح · نوافذ · إمكانية وصول).
  لا تأخذ منها ألوانًا ولا تايبوغرافي ولا تخطيطًا. أعد تلوين أي مكوّن بـ tokens المشروع.
- **UI UX Pro Max:** تجاهل وحدات «لوحات الألوان» و«اقتران الخطوط» و«أنماط UI» — الهوية محسومة.
- **Motion:** للحركات المدفوعة بأحداث وقت التشغيل فقط. الثابتة تبقى CSS.

## ممنوع
- Lorem ipsum — استخدم النصوص العربية الحقيقية من `docs/design-brief.md`
- ذكر نطاق التغطية على اللاندينج، أو ادّعاء الشمول («كل موادك، كل جامعاتك»)
- عبارات تسويقية بلا معنى قابل للقياس
- إيموجي ✨ أو لغة «سحر الذكاء الاصطناعي»
- `<div onclick>` بدل `<button>`

## الاتجاه البصري
- **تايبوغرافي عريض جدًا للعناوين**: وزن 700، تتبّع سالب `-0.03em`. عنوان واحد يهيمن على الشاشة.
- **بطاقات كبيرة** (`--r-lg` / `--r-xl`) — رقم واحد لكل بطاقة + زر سهم دائري في الزاوية السفلية.
- فراغ سخي · حدود خفيفة · ظلال شبه معدومة في الوضع الفاتح.

### تدرّجات الأسطح — ثلاثة فقط
`--tint-aubergine` · `--tint-amber` · `--tint-walnut` — خلفيات بطاقات فقط.
**ممنوع اشتقاق تدرّج أخضر** — الأخضر حالة لا سطح.

### الخربشة
خط يدوي تحت كلمة واحدة في العنوان · SVG · اللون `--warmth` (#916140) · `stroke-width: 4`
(سُمك 4 يحقق قاعدة «الجوزي لا يُستخدم رفيعًا») · **مرة واحدة لكل شاشة كحد أقصى**.

### الـ3D — البيكانة فقط
الأصل الوحيد المسموح: **بيكانة مرندَرة بخامة جوزية دافئة وإضاءة استوديو ناعمة**.
ممنوع: فقاعات عامة · أشكال بلاستيكية لامعة · أي لون خارج اللوحة.
- **يعيش في:** Hero اللاندينج · فواصل أقسام الموقع · الحالات الفارغة · شاشات النجاح
- **يُمنع منعًا باتًا في:** شاشة الجلسة · صفحة المادة · الداشبورد اليومي
- **الأداء:** WebP/AVIF · مقاسات متعددة · `fetchpriority="high"` لصورة الـHero فقط · `loading="lazy"` لما دونها
- **للإطلاق:** بيكانة واحدة في الـHero تكفي. بقية العائلة بعد الإطلاق.

**القاعدة الحاكمة: الدفء في الخارج، التقشّف في الداخل.**
الموقع التسويقي غنيّ ودافئ. الجلسة داكنة صامتة خالية من أي زينة.
